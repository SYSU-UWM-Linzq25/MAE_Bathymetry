requests>=2.31
numpy>=1.24
rasterio>=1.3
# Install GDAL Python bindings matching the GDAL library on the HPC system.
# GeoPandas/Pandas/PyProj/Shapely are not required; vector geometry and CRS transforms use GDAL/OGR/OSR.
# Example: conda install -c conda-forge gdal rasterio requests
