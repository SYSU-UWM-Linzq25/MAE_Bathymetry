--- a/main_pretrain_dem.py
+++ b/main_pretrain_dem.py
@@
 def freeze_for_decoder_adaptation(model, last_n_blocks: int = 0):
@@
-    # print summary
-    total, trainable = 0, 0
-    for _, p in model.named_parameters():
+    # print summary + safety check
+    total, trainable = 0, 0
+    trainable_names = []
+    for name, p in model.named_parameters():
         n = p.numel()
         total += n
         if p.requires_grad:
             trainable += n
+            trainable_names.append(name)
     print(f"[FREEZE] total params={total:,}, trainable params={trainable:,}")
+
+    allowed_prefix = (
+        "decoder_embed", "mask_token", "decoder_pos_embed",
+        "decoder_blocks", "decoder_norm", "decoder_pred"
+    )
+    if last_n_blocks <= 0:
+        bad = [n for n in trainable_names if not n.startswith(allowed_prefix)]
+        if bad:
+            raise RuntimeError(
+                "[FREEZE ERROR] Encoder-side parameters are still trainable: "
+                + ", ".join(bad[:20])
+            )
+
+    print("[FREEZE] trainable parameter groups:")
+    for n in trainable_names[:50]:
+        print("  ", n)
+    if len(trainable_names) > 50:
+        print(f"  ... ({len(trainable_names)-50} more)")
